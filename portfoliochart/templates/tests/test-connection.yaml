apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "portfoliochart.fullname" . }}-test-connection"
  labels:
    {{- include "portfoliochart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "portfoliochart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
